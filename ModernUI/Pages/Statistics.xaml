<UserControl x:Class="ModernUI.Pages.Statistics"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:accounts="clr-namespace:ModernUI.Pages.Statistic.Accounts"
             xmlns:viewmodels="clr-namespace:Core.ViewModels.Statistics.Accounts;assembly=Core"
             mc:Ignorable="d">
    <Grid>
        <TabControl ItemsSource="{Binding StatisticTabs, Mode=TwoWay}">
            <TabControl.Resources>
                <DataTemplate DataType="{x:Type viewmodels:ParentAccountsViewModel}">
                    <accounts:ParentAccounts/>
                </DataTemplate>
            </TabControl.Resources>

            <TabControl.ItemContainerStyle>
                <Style BasedOn="{StaticResource MetroTabItem}" TargetType="TabItem">
                    <Setter Property="Header" Value="{Binding Header}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected}"/>
                    <Setter Property="controls:ControlsHelper.HeaderFontSize" Value="18"/>
                </Style>
            </TabControl.ItemContainerStyle>
        </TabControl>
    </Grid>
</UserControl>
